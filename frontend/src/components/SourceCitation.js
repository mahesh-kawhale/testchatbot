import React from 'react';

// This component is now completely hidden as per user request
const SourceCitation = ({ sources }) => {
  // Return null to render nothing
  return null;
};

export default SourceCitation;